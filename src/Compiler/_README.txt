Compiler steps:

-- Sanitizer.cs
	Sanitizes the code, stripping comments.

-- Tokenizer.cs
	The lexical analyzer. Reads the code into tokens.

-- Parser.cs
	The syntax analyzer. Reads the tokens into assembly.